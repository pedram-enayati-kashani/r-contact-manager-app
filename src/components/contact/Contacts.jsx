import Contact from "./Contact";

const Contacts = () => {
    return (
        <section className="container">
            <div className="row">
                {/* contact */}
                <Contact/>
            </div>
        </section>
    );
};

export default Contacts;
